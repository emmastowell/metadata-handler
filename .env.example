# Databricks configuration (copy to .env and fill in your values)
# Do not commit .env to version control.
#
# Used by: the app at runtime (local) and by deploy.sh when deploying (bundle vars).

# CLI profile name (from databricks auth login)
DATABRICKS_CONFIG_PROFILE=

# Databricks workspace URL (e.g. https://your-workspace.cloud.databricks.com)
DATABRICKS_HOST=

# Model Serving endpoint name (e.g. databricks-claude-opus-4-5)
DATABRICKS_SERVING_ENDPOINT=
